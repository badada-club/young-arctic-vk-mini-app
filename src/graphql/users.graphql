mutation createUser($input: VkEventInput) {
    createVkUser(input: $input) {
        id
        name
        vkId
    }
}

query getUsers {
    getVkUsers {
        id
        name
        vkId
    }
}